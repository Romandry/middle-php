# Catalog Module

Layers:
- Domain: Aggregates, Entities, Value Objects, Domain Events (no Laravel, no DB)
- Application: Use-cases, handlers, ports (interfaces), DTOs
- Infrastructure: DB repositories, external clients, queue/outbox implementations
- UI: HTTP controllers, requests, resources
